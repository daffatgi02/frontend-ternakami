name: ternakami
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:
  cached_network_image: ^3.3.1
  camera: ^0.11.0+1
  flutter:
    sdk: flutter
  http: ^1.2.0
  http_parser: ^4.0.2
  image_picker: ^1.0.8
  crop: ^0.5.5
  image_gallery_saver: ^2.0.3
  permission_handler: ^11.3.1
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets\gambar\login_image.png
    - assets\gambar\regis.png
    - assets\gambar\onboarding2.png
    - assets\gambar\onboarding1.png
    - assets\gambar\onboarding3.png